# Development Artifacts

- `patch_scripts/`: one-off patch scripts used during iterative development.
- Runtime and training outputs are kept under `runs/`.
- Stable good runners are kept under `runs/good_runners/`.
