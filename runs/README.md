# Runs Directory

- `good_runners/`: stable runner references used for balance diagnostics.
- `rl_experiments/`: archived training runs, benchmarks, diagnostics, and exploratory outputs.
- New balance checks can still be written to fresh `runs/rl_*` folders.
